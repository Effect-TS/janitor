# 12: Upgrade running sessions safely

**What to build:** Operators perform controlled releases while incoming instructions remain durable, resume supported sessions afterward, and preserve incompatible sessions for repair.

**Blocked by:** 10: Recover missed events and interrupted platform delivery; 11: Preserve or clean up work when repository access changes.

**Status:** needs-triage

Source: [Accepted implementation handoff](../spec.md). Implement this slice within its accepted contracts and evidence limits.

- [x] Create release manifests with independently versioned command/events, native migration sets, Janitor schemas, checkpoint format and running bridge capabilities. Pin build/image/dependency identities and supported combinations.
- [x] Validate outer compatibility metadata and native migration journal before SDK host construction. Reject unknown/newer state, missing metadata on nonempty databases and incompatible checkpoints without mutation or reset.
- [x] Persist migration intent and completion around supported native forward migrations. Handle partial committed migration failure explicitly and test supported restart; do not assume whole-upgrade atomicity or downgrade support.
- [x] Establish a durable maintenance barrier before enumerating sessions, keep authenticated durable intake active, withhold new dispatch and obtain per-runner quiescence using native shutdown semantics that preserve recoverable claims.
- [x] Wait for prior writers and finite operations, commit checkpoints/results or retain uncertainty, and keep holds effective across alarms/restarts. Unreachable is not quiescent; newer disconnection/removal fences outrank stale maintenance release.
- [x] Deploy compatible consumers before new writers and verify actual bridge image/protocol after rollout. Release only matching holds after state, peer and credential checks; retain session model records and current approved secrets.
- [x] Permit rollback only on a tested path supporting actual current state/peers. Otherwise repair forward while blocked; never rewind inputs, publication receipts or whole-state snapshots automatically.
- [x] Exercise the full isolated maintenance path with incoming messages, active model/tool work, old images, restart, partial migration, stale acknowledgments and refused rollback. Surface maintenance/compatibility reasons through existing observation.

## Comments

2026-09-13: Implemented. The runner gains a pinned `release-manifest.json` (command protocol, native migration set with every id, Janitor state format and readable formats, checkpoint manifest and archive formats, bridge protocol, required capabilities and image identity, build identities and readable state families), checked against the compiled bundle and every pinned artifact by `test/ReleaseManifest.test.ts` and published on `/v1/health` with any problems. The bridge image records and advertises its source hash; `checkBridge` requires the manifest's protocol, capabilities and hash on every dispatch and on release, so an old image receives no tool work. The compatibility record moves to format 2 with the state family and a migration intent naming the target; `src/Compatibility.ts` decides, without mutation, between blocked (no record on native state, newer migrations, unreadable format, family without a tested path, an initialization by another target that did not complete with the partial count, or an incompatible checkpoint manifest), plain start, resume of the same target, and the format 1 upgrade, which adds the checkpoint manifest column and backfills the pointer under intent. Checkpoint pointers commit with a manifest that restore validates. The maintenance hold persists first, drains admitted foreground work to its finite timeout so result and checkpoint commit together, holds any fresh model request until shutdown interruption, and answers quiescence and uncertainty; release runs state, checkpoint, model credential and running bridge checks and keeps the hold when any fails, and a disconnection outranks a stale release. Janitor gains migration `0031_maintenance_barrier.sql`, the `AgentMaintenance` service (hold before enumeration, advance driven by the catch-up cron, status, release after verifying runner health, protocol, family and expected release, per-session refusal, re-requested delivery under a distinct handoff key), a dispatch withhold in the handoff, the maintenance reason in session observation and service-token operator routes under `/api/v1/maintenance`. Tests: runner `Compatibility`, `ReleaseManifest`, `Upgrade` and `MaintenanceRepository` (real image: hold during a running command, old image refused, foreign and unsupported checkpoints, restore after Sandbox loss), Janitor `Agent/Maintenance.test.ts` (barrier before enumeration, withheld intake, late sessions, unreachable and stale runners, refused release, disconnection during hold, ordered re-drive) and the acceptance driver (hold during in-flight model work, message accepted under the hold, runner process replacement, verified release, native resumption joining the queued input). The recovery sweep's handoff payload now carries its own idempotency key, since the creation key it reused would have answered from the completed creation execution. Review findings applied: release refuses until every session (including ones started since the last advance) has acknowledged and quiesced; tool admission waits under a hold like model requests; the runner re-checks epoch and fence after its release checks and refuses during a drain; the re-delivery request is enqueued before a hold is marked released; shared bearer-token and list helpers; the event contract is verified on release. Credential rotation across runtime activation and session-pinned model retention are ticket 13's scope and are not exercised here; uncertain publication across a release boundary is not exercised either. Evidence limits: all checks are local (Miniflare, Docker, PostgreSQL); no live cross-version deployment, container rollout or Cloudflare rollback was performed.
